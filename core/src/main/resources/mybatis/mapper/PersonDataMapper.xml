<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="liga.medical.personservice.core.repository.PersonDataRepository">

    <resultMap id="personDataEntity" type="liga.medical.personservice.core.model.PersonDataEntity" autoMapping="true">
        <id property="id" column="id" jdbcType="BIGINT"/>
    </resultMap>

    <select id="findByIds" resultMap="personDataEntity">
        <bind name="" value=""/>
        SELECT id, email, password, role, status, last_name, first_name, birth_dt, age, sex, contact_id, medical_card_id, parent_id
        FROM person_data
        <choose>
            <when test="personIds.isEmpty()">
                ORDER BY recovery_dt DESC
                LIMIT 10;
            </when>
            <otherwise>
                WHERE id IN
                <foreach collection='personIds' item='id' open='(' separator=',' close=')'>
                    #{id, jdbcType=BIGINT}
                </foreach>
            </otherwise>
        </choose>
    </select>
    <select id="findByEmail" resultMap="personDataEntity">
        SELECT pd.*, r.id AS role_id, r.value AS value
        FROM person_data AS pd
            INNER JOIN role_person AS rp ON pd.id = rp.person_data_id
            INNER JOIN role AS r ON r.id = rp.role_id
        WHERE pd.email=#{email};
    </select>


    <insert id="insertAll" useGeneratedKeys="true" keyColumn="id" keyProperty="id">
        INSERT INTO person_data (last_name, first_name, birth_dt, age, sex, contact_id, medical_card_id, parent_id) VALUES
        <foreach collection="personDataList" item="person" separator=",">
            (#{person.lastName, jdbcType=VARCHAR(255)}, #{person.firstName,jdbcType=VARCHAR(255)},
            #{person.birthDt,jdbcType=DATE}, #{person.age,jdbcType=SMALLINT},
             #{person.sex,jdbcType=CHAR}, #{person.contactId,jdbcType=BIGINT},
             #{person.medicalCardId,jdbcType=BIGINT}, #{person.parentId,jdbcType=BIGINT})
        </foreach>
    </insert>

    <update id="updateById" parameterType="liga.medical.personservice.core.model.PersonDataEntity">
        <if test="id != null">
            UPDATE person_data SET
            <if test="lastName != null">
                last_name = #{lastName},
            </if>
            <if test="firstName != null">
                first_name = #{firstName},
            </if>
            <if test="birthDt != null">
                birth_dt = #{birthDt},
            </if>
            <if test="age != null">
                age = #{age},
            </if>
            <if test="sex != null">
                sex = #{sex},
            </if>
            <if test="contactId != null">
                contact_id = #{contactId},
            </if>
            <if test="medicalCardId != null">
                medical_card_id = #{medicalCardId},
            </if>
            <if test="parentId != null">
                parent_id = #{parentId},
            </if>
            WHERE id = #{id};
        </if>
    </update>

    <delete id="deleteById">
        DELETE
        FROM person_data
        WHERE case_id = #{personId,jdbcType=BIGINT};
    </delete>
</mapper>